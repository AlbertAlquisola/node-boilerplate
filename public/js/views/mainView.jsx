var React = require('react'),
    app;

app = React.createClass({
  getInitialState: function() {
    return {};
  },

  render: function() {
    return (<div>rendering in react!</div>);
  }
});

module.exports = app;
